---
/** Publicos: three-column grid for "con quién trabajamos". Mirrors Publico.d.ts. */
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={["publicos", className]}><slot /></div>
