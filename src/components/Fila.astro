---
/** Fila: two fields side by side; one column below 880px. Mirrors Campo.d.ts. */
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={["formulario__fila", className]}><slot /></div>
